[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "LUMIN"
version = "0.0.1"
dynamic = ["requires-python", "dependencies", "classifiers", "entry-points"]

[project.scripts]
lumin = "lumin._launcher:main"